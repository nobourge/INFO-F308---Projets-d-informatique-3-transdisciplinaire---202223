name: Lint

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: isort --check
      runs: |
        isort --check .
    - name: black --check
      runs: |
        black --check .
